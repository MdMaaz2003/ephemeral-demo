replicaCount: 2

image:
  repository: nginx
  tag: latest

service:
  type: ClusterIP
  port: 80

serviceAccount:
  create: false
  name: ""
  annotations: {}
  automount: false

autoscaling:
  enabled: false

httpRoute:
  enabled: false

ingress:
  host: my-app.local

  
